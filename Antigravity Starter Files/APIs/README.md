# APIs (optional)

## Where keys go
1) Duplicate `keys.example.env`
2) Rename the copy to: `keys.env`
3) Paste optional project keys into `keys.env` only

## Rules
- Never commit `keys.env`
- If you add more keys later, add new lines to `keys.example.env` too
- Default camp projects do not require a separate image-generation API key.

## Fill-in knobs (edit later)
- Primary API(s) used in this workspace: none by default
- Rate-limit / usage rules: [FILL]
